.next
node_modules
.env.local
.env
npm-debug.log*
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
htmlcov/

# Local runtime data — task-proof uploads and evidence files written by
# the backend during local dev/testing, never source content
backend/uploads/
backend/evidence_storage/

# Never commit runtime secrets
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example

# Internal planning/reference docs — not part of the shipped repo
docs/
todo.md

# Local Claude Code tool config — not part of the shipped app
.claude/
