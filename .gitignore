.next
node_modules
.env.local
.env
npm-debug.log*
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
backend/.venv/
.coverage
htmlcov/

# Local runtime data — task-proof uploads written by the backend during
# local dev/testing, never source content. Evidence lives in Supabase
# Storage (app.services.evidence_storage), not on local disk.
backend/uploads/

# Never commit runtime secrets
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example

# Internal planning/reference docs — not part of the shipped repo
docs/
todo.md

# Local Claude Code tool config — not part of the shipped app
.claude/
